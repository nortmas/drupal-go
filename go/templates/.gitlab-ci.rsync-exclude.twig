.git
.gitlab-ci.yml
.gitlab-ci.rsync-exclude
.gitignore
README.md
docker-compose.dev.yml

# Safety
*.swp
.idea
.DS_Store

# Exclude files folder to prevent deletion from the endpoint folder.
web/sites/default/files

# Exclude db folder to prevent deletion from the endpoint folder.
db